# HTML (tableau.HTML)

Uniquement pour afficher une page HTML statique :

```bash
python -m http.server 800
```

cd src/tutos

Install :

```bash
npm init -y   
npm install vite --save-dev  
set vite.config.js
```

Lancver le server viteJS :

```bash
npm run dev
```
